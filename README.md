# Local 10 — Union site + notes

This repo is intentionally simple:

- **Union site**: `index.html`, `styles.css`, `main.js`
- **Storage notes** (paste anytime): `notes/`

## Open locally

Just open `index.html` in a browser.

If you want a local server:

```bash
python3 -m http.server 5173
```

Then visit `http://localhost:5173`.

## Notes files

- `notes/thinking.md`
- `notes/game-design.md`
- `notes/union.md`
